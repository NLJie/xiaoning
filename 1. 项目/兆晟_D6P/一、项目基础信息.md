# 1. 基础信息

## 1.1. 挂载远程目录命令

`busybox mount -o nolock -o tcp -o rsize=32768,wsize=32768 192.168.1.69:/tftpboot /mnt/zs_sys_file/test`

`busybox mount -o nolock -o tcp -o rsize=32768,wsize=32768 192.168.1.69:/tftpboot /mnt/sys_data/program/test`

`busybox mount -o nolock -o tcp -o rsize=32768,wsize=32768 192.168.1.69:/tftpboot /mnt/sdcard`


```
ifconfig eth0 192.168.1.18 netmask 255.255.255.0
route add default gw 192.168.1.1
```

# 2. 当前缺陷

1. 没有GPS模块；
2. 云台没有应答确认，云台PT值没有到达确认；